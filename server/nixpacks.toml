# Railway Nixpacks configuration
# This file tells Railway which Node.js version to use

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["echo 'No build step needed for server'"]

[start]
cmd = "node index.js"

